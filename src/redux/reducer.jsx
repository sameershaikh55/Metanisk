// import {} from "./types";

const initialState = {};

const Reducer = (state = initialState, action) => {
	switch (action.type) {
		// case WHAT_WE_OFFER:
		// 	return {
		// 		...state,
		// 		whatWeOfferIS: action.payload,
		// 	};
		default:
			return state;
	}
};
export default Reducer;
